upstream app {
    {% for backend in apps %}
    server {{ backend }};
    {% endfor %}
}

server {
    listen {{ server_port }};
    server_name {{ server_name }};

    location / {
        proxy_pass http://app;
        add_header X-Upstream $upstream_addr;
    }
}
